# ============================================================
# P2 交付代码修复清单（P1 §4.2 二-2：精确 Git commit hash + 逐文件修改说明）
# ============================================================
# 【填写说明】
#   1. 执行：`cd funasr-command-recognition && git rev-parse HEAD`
#   2. 将输出的 40 位 commit hash 填入下方 GIT_COMMIT_HEAD = "..."
#   3. 执行 `git push` 推送到远端后，再确认最终 hash 一致
# ============================================================

GIT_COMMIT_HEAD = "<待填入 git rev-parse HEAD 的 40 位 hash>"

# ============================================================
# 修改文件列表（对应 change_note.md §2.1 代码修复 + §2.2 配置 + §6）
# ============================================================
# 所有路径相对 funasr-command-recognition 仓库根
# funasr_project/P2_project/ 前缀可简写为 P2/
# ============================================================

# ------------------------------------------------------------
# 1. 训练脚本（P2/tools/train_b1_trial.py）
# ------------------------------------------------------------
# 修复 5 处（对应 change_note.md §2.1 表格 1-5 行）
#
# (a) _is_absent_entry() 开头：
#     增加 `if e.get("target_present") is True: return False`
#     → swap_target(target_present=True) 不再被误判 absent → 不再强制置零
#
# (b) main() L508-L524 CAM++ 强制加载机制：
#     adapter.mode == campplus 且非 --debug_data 时，循环 3 次 adapter.load_backend()
#     失败抛 RuntimeError 终止训练，不允许 silent fallback BOOTSTRAP
#     → 保证 B2/B3 训练全程使用真实 CAM++ embedding
#
# (c) 删除 L517 局部 `import time`：
#     仅保留文件顶部全局 `import time`，避免 CAM++ 重试循环中出现
#     UnboundLocalError: local variable 'time' referenced before assignment
#
# (d) main() 开头 sys.path 显式注入：
#     将 P4_project/artifacts/models/speakerlab_source 与 P4_project 加入 sys.path
#     确保 speakerlab 模块可找到（CAM++ 依赖）
#
# (e) CLI 解析：
#     新增 --init_checkpoint 参数，从 B1 最优权重 warm-start 初始化 B2/B3
#     → 加速收敛 + PRESENT 场景提取质量基线继承
# ------------------------------------------------------------
P2/tools/train_b1_trial.py
  - _is_absent_entry: target_present=True 优先保护
  - CAM++ 强制加载（3 次重试失败 RuntimeError，无 fallback）
  - 删除局部 import time（L517）
  - sys.path 注入 speakerlab / P4_project
  - 新增 --init_checkpoint warm-start 参数
  - scene_mode {b1, b2, b3} 与 _is_swap_entry 判定逻辑通用

# ------------------------------------------------------------
# 2. 评测脚本（P2/tools/evaluate_tse.py）
# ------------------------------------------------------------
# 修复 4 处（对应 change_note.md §2.1 表格 6-9 行）
#
# (a) _is_absent_entry 与训练一致：target_present=True 优先 return False
# (b) EnrollmentAdapter 强制 CAM++ 加载：训练脚本一致的失败抛错机制
# (c) swap 评测 wrong_enroll 路径：manifest 无 swap_enrollment / swap_enroll_wav
#     字段时跳过 swap choice_accuracy 评测（写 null），避免 FileNotFoundError
# (d) soundfile.write WAV 输出：磁盘空间不足时 try-except 跳过写入并告警
#     防止评测后半段因磁盘不足整体失败
# ------------------------------------------------------------
P2/tools/evaluate_tse.py
  - _is_absent_entry: 与训练脚本一致的 target_present 保护
  - EnrollmentAdapter: 强制 CAM++，不允许 silent fallback
  - swap wrong_enroll: 缺字段时跳过硬指标，不报错
  - WAV 写入: 磁盘不足时 try-except，不中断评测
  - scene_mode CLI 参数支持（b1/b2/b3 分场景评测）

# ------------------------------------------------------------
# 3. CAM++ 声纹后端（P2/src/tse/campplus_backend.py）
# ------------------------------------------------------------
# 修复 2 处（对应 change_note.md §2.1 表格 10 行）
#
# (a) embedding_size: 默认 192，匹配 CAM++ 预训练权重形状
#     → 避免 shape mismatch 加载失败
# (b) _resolve_weight_path: 权重路径解析修正，兼容 speakerlab_source
#     多目录位置 + P4_project 相对路径
# ------------------------------------------------------------
P2/src/tse/campplus_backend.py
  - embedding_size = 192（匹配预训练权重维度）
  - _resolve_weight_path 多候选路径解析修正

# ------------------------------------------------------------
# 4. 训练配置文件（P2/configs/）
# ------------------------------------------------------------
P2/configs/tse_b2_v2_campplus.yaml    # B2 v2 配置：scene_mode=b2，真实 CAM++，B1 warm-start
                                      # → 训练时因 warm-start 负迁移卡住，作废 v2
P2/configs/tse_b2_v3_campplus.yaml    # B2 v3 配置：lambda_act=2.0 / zero_ref_kappa=1e-2
                                      # → 仍卡 -56.99 dB，作废 v3
P2/configs/tse_b2_v4_campplus.yaml    # B2 v4 配置：NO warm-start scratch, 10K 步
                                      # → 仍局部最优，作废 v4
                                      # → 最终决策：B2 复用 B3 v3 权重（见 REUSE_B3_NOTE.txt）
P2/configs/tse_b3_v3.yaml             # B3 v3 交付版 ★
                                      #   absent_loss_scale=0.05（absent 梯度砍 95%）
                                      #   lambda_sisdr=2.0（SI-SDR 提取强化）
                                      #   lambda_act=1.0（帧级活动检测适中）
                                      #   lr_warmup_steps=500（warm-start 长 warmup）
                                      #   seed=20260813（可复现）
P2/configs/tse_b3_v4.yaml             # B3 v4 作废保留（勿交付）
                                      #   lambda_act=2.0 + zero_ref_kappa=1e-4 过度抑制
                                      #   → energy_ratio 从 0.00108 跌到 1.57e-9（再近静音）
                                      #   → 仅保留作回归对照

# ------------------------------------------------------------
# 5. 交付文档 + 打包脚本
# ------------------------------------------------------------
P2/change_note.md                     # ★ P1 §4.2 第 10 条交付件
                                      #   §一：旧 B3 近静音退化根因（4 层面）
                                      #   §二：修复点汇总（代码+配置+训练流程）
                                      #   §三：v2.0 指标对照（B1/B2/B3 新 vs 旧）
                                      #   §3.4：B2 复用 B3 v3 架构合法性 5 条证明
                                      #   §四：P1 §4.2 每一项 → 交付文件精确索引
                                      #   §五：P3 接入指引（§6 流程）
                                      #   §六：代码修改文件清单目录

P2/tools/pack_p2_delivery.py          # ★ 打包脚本
                                      #   按 P1 §4.2 清单逐条校验
                                      #   输出：P2_DELIVERY_FOR_P3_20260811/
                                      #         + DELIVERY_MANIFEST.json
                                      #         + fix_list.txt（本文件）
                                      #         + train_commands.txt
                                      #         + change_note.md

# ------------------------------------------------------------
# 6. 运行环境配置（复现训练/评测）
# ------------------------------------------------------------
#   · GPU: AutoDL A100-40G（CUDA 11.8）
#   · PyTorch: 2.1.2+cu118
#   · Python: 3.10
#   · 声纹依赖：P4_project/artifacts/models/speakerlab_source/
#               + P4_project/artifacts/models/campplus_cn_common.bin
#               → sys.path 显式注入（见 train/eval 脚本 main 开头）
#   · 真实声纹强制：EnrollmentAdapter.load_backend() 必须成功
#                   → 失败立即 RuntimeError，不 silent fallback BOOTSTRAP
# ------------------------------------------------------------
