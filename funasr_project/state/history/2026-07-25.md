# 2026-07-25 P2 工作日志

## 今日动作

1. **资产清理**：删除 aishell1 重复 .tar.gz（57 个，1.94 GB）+ .part 残留 + __pycache__，D 盘 55.1 → 56.96 GB
2. **P2-06 双输出 TSE 最小模型 + 随机张量 smoke**（今日核心交付）
   - 新建：`configs/p2/tse_smoke.yaml`、`src/tse/losses.py`、`src/tse/model.py`、`src/tse/api.py`、`src/tse/__init__.py`、`tests/p2/smoke_tse_random.py`
   - 架构：STFT(512/128) → input_proj(257→256) → FiLM（1+0.1·tanh(γ)，隐空间注入）→ LSTM×2(256) → 双 sigmoid 掩码头 → ISTFT → 硬投影 ŝ+r̂=x
   - 参数量 1,349,634（<3M 预期）
   - 中途修复 2 个 bug：T<n_fft 短波形 STFT 反射填充失败（补零保护）；投影基准误用补零后输入（改 x_orig）
   - 讨论确认：硬投影不阻断梯度（雅可比特征值 1/0，分解方向梯度无损）
3. **建档**：state/P2_progress.json（含决策 D-2026-07-25-01：不迁移目录）、reports/P2_machine_inventory.md、state/P2_dependency_register.json

## 运行命令

```powershell
python tests/p2/smoke_tse_random.py --device auto   # EXIT_CODE=0，总体 PASS
```

## 产物

- reports/smoke/tse_random_forward.json（四档长度全过，一致性误差 ~0，16 参数张量梯度有限非零）
- logs/p2/smoke_tse_random.log

## 异常 / 阻塞

- 本机无 CUDA：GPU 显存验收 BLOCKED_EXTERNAL（已显式记录，非 silent skip）
- psutil 未装：CPU 内存检查项记 N/A

4. **P2-08 注册条件验证 PASS**：tests/p2/test_tse_conditioning.py 六检查全过；中途修复测试设计 bug（randperm 存在不动点 → 改循环移位）
5. **pytest 薄包装落地**：tests/p2/test_dual_tse_smoke.py，`python -m pytest tests/p2 -v` → 12 passed + 1 skipped（vram 项 cuda_unavailable 显式跳过）；pytest 9.1.1 经清华镜像安装（官方源与 7897 代理均不通）；修复包装层 scale_sensitive_l1 漏传 kappa 的签名错误
6. **P2-07 DEBUG_ONLY 小语音集 PASS**：p2_tools/build_debug_mixtures.py 生成 12 条 4s 混合（trials 4 说话人 × partial25/full100/swap50，full100=4、enrollment-swap=4 达标）；产物 artifacts/p2/debug_mixtures_v0/（stem + activity.npy + manifest.jsonl + SHA256SUMS.txt + _meta.json）；验收 tests/p2/test_debug_mixtures.py 6 项全过（闭合<1e-6、防削波、掩码一致、真实 mixture 可过模型）；全套 pytest 18 passed + 1 skipped

## 下一步（按优先级）

1. pytest 薄包装（tests/p2/test_dual_tse_smoke.py）
3. P2-07 DEBUG_ONLY 小语音集（p2_tools/build_debug_mixtures.py）
4. losses.py 升级 05A 冻结五项公式 + model 暴露投影前输出（P2-09 前置）
