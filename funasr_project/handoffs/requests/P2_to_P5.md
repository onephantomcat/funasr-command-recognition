# P2 → P5 接口请求单

- 发起方：P2（TSE 模块）  接收方：P5（管线/集成模块）
- 日期：2026-07-30

## 需要什么

| 交付 | 内容 | 最晚需要日期 |
|---|---|---|
| `pipeline_contract_v1` | TSE 集成契约：输入输出接口（采样率/帧长/流式或离线）、延迟预算、与拒识模块的级联顺序、失败回退策略 | 2026-08-09（P2-16 前置，联调窗口 8/10–15） |

## schema / version

- P2 侧已实现 `extract_target(command_wav, enroll_embedding)` 双输出接口（src/tse/api.py），契约应对接该签名或提出修改请求
- 需明确 ABSENT 场景下 TSE 输出近静音时管线的预期动作（05A：EMPTY 路由，不靠固定能量阈值）

## 缺失时 P2 继续做什么

- extract_target API 契约文档化 + 边界测试（极短输入/全零输入已有 smoke 覆盖）

## 哪些正式结论会被阻塞

- P2-16 冻结主模型交接无法完成 → 端到端（TSE→ASR→拒识）集成联调阻塞
